<Window x:Class="Alpha6Ops.Desktop.MainWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:local="clr-namespace:Alpha6Ops.Desktop" xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework" Title="Alpha 6 OPS" Icon="Assets/Alpha6OPS.ico" Width="1536" Height="1024" MinWidth="640" MinHeight="480" ResizeMode="CanResize" WindowStartupLocation="CenterScreen" WindowStyle="None" Background="#04090D" Foreground="#F1F4F7" UseLayoutRounding="True" TextOptions.TextFormattingMode="Display">
  <shell:WindowChrome.WindowChrome>
    <shell:WindowChrome CaptionHeight="38" ResizeBorderThickness="7" GlassFrameThickness="0" CornerRadius="0" UseAeroCaptionButtons="False" />
  </shell:WindowChrome.WindowChrome>
  <Window.Resources>
    <Style x:Key="OpsCaptionButton" TargetType="Button">
      <Setter Property="Width" Value="46" />
      <Setter Property="Height" Value="38" />
      <Setter Property="Padding" Value="0" />
      <Setter Property="Margin" Value="0" />
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="Foreground" Value="#C7D2DA" />
      <Setter Property="BorderThickness" Value="0" />
      <Setter Property="FontFamily" Value="Segoe MDL2 Assets" />
      <Setter Property="FontSize" Value="10" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border Background="{TemplateBinding Background}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
      <Style.Triggers>
        <Trigger Property="IsMouseOver" Value="True">
          <Setter Property="Background" Value="#25323B" />
          <Setter Property="Foreground" Value="White" />
        </Trigger>
        <Trigger Property="IsPressed" Value="True">
          <Setter Property="Opacity" Value="0.7" />
        </Trigger>
      </Style.Triggers>
    </Style>
    <Style x:Key="OpsCloseCaptionButton" TargetType="Button" BasedOn="{StaticResource OpsCaptionButton}">
      <Style.Triggers>
        <Trigger Property="IsMouseOver" Value="True">
          <Setter Property="Background" Value="#C42B1C" />
          <Setter Property="Foreground" Value="White" />
        </Trigger>
      </Style.Triggers>
    </Style>
    <DataTemplate x:Key="AlertTemplate">
      <Button Style="{StaticResource OpsLink}" Padding="13,10" HorizontalContentAlignment="Stretch" Tag="{Binding}" Click="Alert_Click">
        <Grid>
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="27" />
            <ColumnDefinition />
          </Grid.ColumnDefinitions>
          <TextBlock Text="{Binding Icon}" Foreground="{Binding Color}" FontFamily="Segoe MDL2 Assets" FontSize="19" />
          <StackPanel Grid.Column="1">
            <Grid>
              <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="Auto" />
              </Grid.ColumnDefinitions>
              <TextBlock Text="{Binding Title}" Foreground="{Binding Color}" FontFamily="Bahnschrift SemiCondensed" FontSize="13" TextWrapping="Wrap" />
              <TextBlock Grid.Column="1" Text="{Binding Age}" Style="{StaticResource OpsCaption}" FontSize="9" Margin="6,0,0,0" />
            </Grid>
            <TextBlock Text="{Binding Summary}" Foreground="#B2BEC9" FontSize="12" TextWrapping="Wrap" Margin="0,5,0,0" LineHeight="16" />
          </StackPanel>
        </Grid>
      </Button>
    </DataTemplate>
    <DataTemplate x:Key="MessageTemplate">
      <Button Style="{StaticResource OpsLink}" Foreground="#EEF2F5" Padding="12,8" HorizontalContentAlignment="Stretch" Tag="{Binding}" Click="Message_Click">
        <Grid>
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="26" />
            <ColumnDefinition />
            <ColumnDefinition Width="Auto" />
          </Grid.ColumnDefinitions>
          <TextBlock Text="{Binding Icon}" FontFamily="Segoe MDL2 Assets" Foreground="{Binding Color}" FontSize="16" />
          <StackPanel Grid.Column="1">
            <TextBlock Text="{Binding Title}" FontSize="13" TextWrapping="Wrap" />
            <TextBlock Text="{Binding Subtitle}" Foreground="#91A4B4" FontSize="11" Margin="0,3,0,0" TextWrapping="Wrap" />
          </StackPanel>
          <TextBlock Grid.Column="2" Text="{Binding ReadLabel}" Foreground="#92A2B0" FontSize="9" Margin="6,2,0,0" />
        </Grid>
      </Button>
    </DataTemplate>
  </Window.Resources>
  <Grid Background="#04090D">
    <Grid.RowDefinitions>
      <RowDefinition Height="38" />
      <RowDefinition />
    </Grid.RowDefinitions>
    <Border Grid.Row="0" Background="#04090D" BorderBrush="#172630" BorderThickness="0,0,0,1">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <StackPanel Orientation="Horizontal" Margin="13,0,0,0" VerticalAlignment="Center">
          <Image Source="Assets/Alpha6OPS.ico" Width="18" Height="18" />
          <Button x:Name="AccountHeaderButton" Content="ACCOUNT · LOCAL PREVIEW" Style="{StaticResource OpsLink}" Click="Account_Click" Margin="16,0,0,0" Padding="10,3" MaxWidth="540" shell:WindowChrome.IsHitTestVisibleInChrome="True" AutomationProperties.Name="Account and workspaces" />
        </StackPanel>
        <StackPanel Grid.Column="1" Orientation="Horizontal" shell:WindowChrome.IsHitTestVisibleInChrome="True">
          <Button Style="{StaticResource OpsCaptionButton}" Click="MinimizeWindow_Click" AutomationProperties.Name="Minimize Alpha 6 OPS">
            <Viewbox Width="12" Height="12">
              <Canvas Width="12" Height="12">
                <Path Data="M 1,9 L 11,9" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1.2" />
              </Canvas>
            </Viewbox>
          </Button>
          <Button Style="{StaticResource OpsCaptionButton}" Click="ToggleMaximizeWindow_Click" AutomationProperties.Name="Maximize or restore Alpha 6 OPS">
            <Grid Width="12" Height="12">
              <Rectangle x:Name="MaximizeIcon" Width="10" Height="10" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1.1" />
              <Grid x:Name="RestoreIcon" Visibility="Collapsed">
                <Path Data="M 3,1 L 11,1 L 11,9 L 9,9 M 1,3 L 9,3 L 9,11 L 1,11 Z" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1.1" Fill="#04090D" />
              </Grid>
            </Grid>
          </Button>
          <Button Style="{StaticResource OpsCloseCaptionButton}" Click="CloseWindow_Click" AutomationProperties.Name="Close Alpha 6 OPS">
            <Viewbox Width="12" Height="12">
              <Canvas Width="12" Height="12">
                <Path Data="M 1.5,1.5 L 10.5,10.5 M 10.5,1.5 L 1.5,10.5" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1.2" />
              </Canvas>
            </Viewbox>
          </Button>
        </StackPanel>
      </Grid>
    </Border>
    <Viewbox x:Name="DashboardViewport" Grid.Row="1" Stretch="Uniform" StretchDirection="Both" HorizontalAlignment="Center" VerticalAlignment="Center">
    <Grid x:Name="DashboardRoot" Width="1888" Height="1026" Margin="16,10,16,6">
    <Grid.RowDefinitions>
      <RowDefinition x:Name="HeaderRow" Height="110" />
      <RowDefinition />
      <RowDefinition Height="36" />
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition x:Name="NavigationColumn" Width="204" />
      <ColumnDefinition x:Name="NavigationGap" Width="16" />
      <ColumnDefinition />
    </Grid.ColumnDefinitions>
    <Rectangle x:Name="HeaderLogo" Grid.ColumnSpan="3" Width="254" Height="98" HorizontalAlignment="Left" VerticalAlignment="Top" RenderOptions.BitmapScalingMode="HighQuality" AutomationProperties.Name="Alpha 6 OPS — Operations Control Center" ToolTip="Alpha 6 OPS — Flight Simulation Operations">
      <Rectangle.Fill>
        <ImageBrush ImageSource="Assets/Dashboard/alpha6-ops-logo.png" Viewbox="0,0.13,1,0.72" Stretch="Uniform" />
      </Rectangle.Fill>
    </Rectangle>
    <Grid x:Name="HeaderStatusGrid" Grid.ColumnSpan="3" Margin="276,0,0,12">
      <Grid.ColumnDefinitions>
        <ColumnDefinition />
        <ColumnDefinition />
        <ColumnDefinition />
        <ColumnDefinition />
      </Grid.ColumnDefinitions>
      <Button x:Name="ConnectionBadge" Style="{StaticResource OpsButton}" Background="#091219" BorderBrush="#293A46" Margin="0,0,10,0" Padding="10,8" Click="QuickConnect_Click" AutomationProperties.Name="Simulator connection" AutomationProperties.HelpText="Connect to MSFS 2024, or view connection controls when a session or replay is active.">
        <Button.ToolTip>
          <StackPanel MaxWidth="320">
            <TextBlock Text="{Binding Text, ElementName=ConnectionActionText}" Foreground="{StaticResource OpsYellow}" />
            <TextBlock Text="{Binding Text, ElementName=ConnectionText}" Margin="0,5,0,0" TextWrapping="Wrap" />
          </StackPanel>
        </Button.ToolTip>
        <Grid x:Name="ConnectionCardContent" HorizontalAlignment="Center" VerticalAlignment="Center">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="24" />
            <ColumnDefinition Width="Auto" />
          </Grid.ColumnDefinitions>
          <Ellipse x:Name="ConnectionDot" Width="13" Height="13" Fill="#80919D" />
          <StackPanel Grid.Column="1" VerticalAlignment="Center">
            <TextBlock x:Name="ConnectionBadgeText" Text="DISCONNECTED" FontFamily="Bahnschrift SemiCondensed" FontSize="14" MaxWidth="110" TextWrapping="Wrap" TextAlignment="Center" AutomationProperties.LiveSetting="Polite" />
            <TextBlock x:Name="ConnectionSourceText" Text="MSFS 2024" Style="{StaticResource OpsCaption}" FontSize="12" TextAlignment="Center" Margin="0,4,0,0" />
            <TextBlock x:Name="ConnectionActionText" FontSize="10" Foreground="{StaticResource OpsYellow}" TextAlignment="Center" Margin="0,4,0,0">
              <TextBlock.Style>
                <Style TargetType="TextBlock">
                  <Setter Property="Text" Value="{Binding SimulatorReadyAction, RelativeSource={RelativeSource AncestorType=Window}}" />
                  <Style.Triggers>
                    <DataTrigger Binding="{Binding IsEnabled, ElementName=ConnectButton}" Value="False">
                      <Setter Property="Text" Value="VIEW CONTROLS" />
                    </DataTrigger>
                  </Style.Triggers>
                </Style>
              </TextBlock.Style>
            </TextBlock>
          </StackPanel>
        </Grid>
      </Button>
      <Button x:Name="HeaderDispatchButton" Grid.Column="1" Style="{StaticResource OpsButton}" Background="#091219" BorderBrush="#22333F" Tag="Dispatch" Click="Module_Click" Margin="0,0,10,0" Padding="10,8" ToolTip="Open the dispatch desk">
        <StackPanel x:Name="DispatchCardContent" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
          <TextBlock Text="" FontFamily="Segoe MDL2 Assets" Foreground="{StaticResource OpsYellow}" FontSize="26" Margin="0,0,12,0" />
          <StackPanel>
            <TextBlock Text="DISPATCH" FontSize="14" TextAlignment="Center" />
            <TextBlock Text="LOCAL DESK" Foreground="#A5B3BE" FontSize="12" TextAlignment="Center" Margin="0,5,0,0" />
          </StackPanel>
        </StackPanel>
      </Button>
      <Button x:Name="LocalWeatherButton" Grid.Column="2" Style="{StaticResource OpsButton}" Background="#091219" BorderBrush="#22333F" Click="LocalWeather_Click" Margin="0,0,10,0" Padding="10,8" HorizontalContentAlignment="Stretch">
        <StackPanel x:Name="WeatherCardContent" VerticalAlignment="Center">
          <TextBlock x:Name="WeatherLocationText" Text="LOCAL WEATHER" Foreground="#A5B3BE" FontSize="12" TextAlignment="Center" TextWrapping="Wrap" />
          <StackPanel x:Name="WeatherReadingContent" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,3,0,0">
            <local:WeatherConditionIcon x:Name="WeatherIcon" Width="30" Height="30" Margin="0,0,9,0" VerticalAlignment="Center" IsHitTestVisible="False" />
            <TextBlock x:Name="WeatherTemperatureText" Text="—" FontFamily="Bahnschrift SemiCondensed" FontSize="25" VerticalAlignment="Center" />
          </StackPanel>
          <TextBlock x:Name="WeatherConditionText" Text="LOCATING…" Foreground="#9FB0BF" FontSize="11" Margin="0,3,0,0" TextAlignment="Center" TextWrapping="Wrap" />
        </StackPanel>
      </Button>
      <Border x:Name="HeaderClockPanel" Grid.Column="3" Style="{StaticResource OpsPanel}" Padding="10,8">
        <Grid x:Name="ClockCardContent" VerticalAlignment="Center">
          <Grid.ColumnDefinitions><ColumnDefinition /><ColumnDefinition Width="21" /><ColumnDefinition /></Grid.ColumnDefinitions>
          <StackPanel x:Name="LocalClockGroup" VerticalAlignment="Center">
            <TextBlock x:Name="LocalClockText" Text="00:00:00 LOCAL" FontFamily="Bahnschrift SemiCondensed" FontSize="22" TextAlignment="Center" />
            <TextBlock x:Name="LocalClockDateText" Style="{StaticResource OpsCaption}" FontSize="11" Margin="0,5,0,0" TextAlignment="Center" />
          </StackPanel>
          <Border x:Name="ClockDivider" Grid.Column="1" Width="1" Background="#293C49" />
          <StackPanel x:Name="ZuluClockGroup" Grid.Column="2" VerticalAlignment="Center">
            <TextBlock x:Name="ClockText" Text="00:00:00 Z" FontFamily="Bahnschrift SemiCondensed" FontSize="22" Foreground="{StaticResource OpsYellow}" TextAlignment="Center" />
            <TextBlock x:Name="ClockDateText" Style="{StaticResource OpsCaption}" FontSize="11" Margin="0,5,0,0" TextAlignment="Center" />
          </StackPanel>
        </Grid>
      </Border>
    </Grid>
    <DockPanel Grid.Row="1" Margin="0,0,0,8">
      <StackPanel x:Name="NavigationProfile" DockPanel.Dock="Bottom" Margin="0,23,0,-6">
        <Border Style="{StaticResource OpsPanel}" Padding="14,13">
          <StackPanel>
            <StackPanel Orientation="Horizontal">
              <Border Width="40" Height="40" CornerRadius="20" Background="#24313B" BorderBrush="#526574" BorderThickness="1">
                <TextBlock x:Name="PilotInitialsText" Text="A6" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold" Foreground="{StaticResource OpsYellow}" />
              </Border>
              <StackPanel Margin="11,0,0,0" VerticalAlignment="Center">
                <TextBlock x:Name="PilotDisplayText" Text="Your flight deck" FontSize="14" FontWeight="SemiBold" />
                <TextBlock x:Name="AccountWorkspaceText" Text="Alpha 6 Virtual" Style="{StaticResource OpsCaption}" FontSize="10" Margin="0,4,0,0" />
              </StackPanel>
            </StackPanel>
            <TextBlock x:Name="AccountSessionStatusText" Text="●  LOCAL SESSION" Foreground="#6BCB65" FontSize="10" Margin="51,8,0,0" />
          </StackPanel>
        </Border>
        <TextBlock x:Name="NavigationTagline" Text="YOU FLY THE AIRPLANE.&#xA;WE RUN THE AIRLINE." FontFamily="Bahnschrift SemiCondensed" FontSize="12" FontWeight="SemiBold" Foreground="#CBD6E0" LineHeight="19" TextWrapping="NoWrap" Margin="15,19,0,0" />
        <Rectangle Fill="{StaticResource OpsYellow}" Height="2" Width="27" HorizontalAlignment="Left" Margin="15,12,0,0" />
      </StackPanel>
      <ScrollViewer VerticalScrollBarVisibility="Disabled" HorizontalScrollBarVisibility="Disabled" PanningMode="None">
        <StackPanel x:Name="NavigationButtons">
          <Button x:Name="DashboardNavButton" Style="{StaticResource OpsNav}" Click="Dashboard_Click">
            <StackPanel Orientation="Horizontal">
              <TextBlock Text="" FontFamily="Segoe MDL2 Assets" FontSize="22" Width="43" />
              <TextBlock Text="DASHBOARD" FontWeight="SemiBold" VerticalAlignment="Center" />
            </StackPanel>
          </Button>
          <Button x:Name="FlightTrackingNavButton" Style="{StaticResource OpsNav}" Tag="FlightTracking" Click="Module_Click" AutomationProperties.Name="FLIGHT TRACKING">
            <StackPanel Orientation="Horizontal">
              <TextBlock Text="" FontFamily="Segoe MDL2 Assets" FontSize="21" Width="43" />
              <TextBlock Text="FLIGHT TRACKING" VerticalAlignment="Center" />
            </StackPanel>
          </Button>
          <Button Style="{StaticResource OpsNav}" Tag="Operations" Click="Module_Click">
            <StackPanel Orientation="Horizontal">
              <TextBlock Text="" FontFamily="Segoe MDL2 Assets" FontSize="21" Width="43" />
              <TextBlock Text="OPERATIONS" VerticalAlignment="Center" />
            </StackPanel>
          </Button>
          <Button Style="{StaticResource OpsNav}" Tag="Aircraft" Click="Module_Click">
            <StackPanel Orientation="Horizontal">
              <TextBlock Text="" FontFamily="Segoe MDL2 Assets" FontSize="21" Width="43" />
              <TextBlock Text="AIRCRAFT" VerticalAlignment="Center" />
            </StackPanel>
          </Button>
          <Button Style="{StaticResource OpsNav}" Tag="Maintenance" Click="Module_Click">
            <StackPanel Orientation="Horizontal">
              <TextBlock Text="" FontFamily="Segoe MDL2 Assets" FontSize="21" Width="43" />
              <TextBlock Text="MAINTENANCE" VerticalAlignment="Center" />
            </StackPanel>
          </Button>
          <Button Style="{StaticResource OpsNav}" Tag="Crews" Click="Module_Click">
            <StackPanel Orientation="Horizontal">
              <Viewbox Width="24" Height="24" Margin="0,0,19,0">
                <Canvas Width="24" Height="24">
                  <Path Data="M 7,6 L 17,6 L 15,3.5 L 9,3.5 Z M 7,7.8 L 17,7.8" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1.4" StrokeLineJoin="Round" />
                  <Ellipse Canvas.Left="8.5" Canvas.Top="7" Width="7" Height="7" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1.4" />
                  <Path Data="M 4.5,21 C 5,16.3 7.7,14.4 12,14.4 C 16.3,14.4 19,16.3 19.5,21 M 9.3,15.1 L 12,18 L 14.7,15.1" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1.4" StrokeStartLineCap="Round" StrokeEndLineCap="Round" StrokeLineJoin="Round" />
                </Canvas>
              </Viewbox>
              <TextBlock Text="CREWS" VerticalAlignment="Center" />
            </StackPanel>
          </Button>
          <Button Style="{StaticResource OpsNav}" Tag="Passengers" Click="Module_Click">
            <StackPanel Orientation="Horizontal">
              <TextBlock Text="" FontFamily="Segoe MDL2 Assets" FontSize="21" Width="43" />
              <TextBlock Text="PASSENGERS" VerticalAlignment="Center" />
            </StackPanel>
          </Button>
          <Button Style="{StaticResource OpsNav}" Tag="Network" Click="Module_Click">
            <StackPanel Orientation="Horizontal">
              <TextBlock Text="" FontFamily="Segoe MDL2 Assets" FontSize="21" Width="43" />
              <TextBlock Text="NETWORK" VerticalAlignment="Center" />
            </StackPanel>
          </Button>
          <Button Style="{StaticResource OpsNav}" Tag="Weather" Click="Module_Click">
            <StackPanel Orientation="Horizontal">
              <Viewbox Width="24" Height="24" Margin="0,0,19,0">
                <Canvas Width="24" Height="24">
                  <Ellipse Canvas.Left="4.5" Canvas.Top="3.5" Width="7" Height="7" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1.35" />
                  <Path Data="M 8,1 L 8,2 M 8,12 L 8,14 M 1,7 L 3,7 M 13,7 L 15,7 M 3,2 L 4.3,3.3 M 11.7,10.7 L 13,12 M 13,2 L 11.7,3.3 M 4.3,10.7 L 3,12" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1.35" StrokeStartLineCap="Round" StrokeEndLineCap="Round" />
                  <Path Data="M 5,20 L 18.5,20 C 21,20 22.5,18.4 22.5,16.3 C 22.5,14.2 20.8,12.6 18.7,12.6 C 18.1,12.6 17.5,12.7 17,13 C 16.2,10.7 14.3,9.5 12,9.5 C 9.2,9.5 7,11.5 6.7,14.2 C 6.2,14 5.7,13.9 5.1,13.9 C 3.2,13.9 1.7,15.2 1.7,17 C 1.7,18.7 3.1,20 5,20 Z" Fill="#04090D" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1.35" StrokeLineJoin="Round" />
                </Canvas>
              </Viewbox>
              <TextBlock Text="WEATHER" VerticalAlignment="Center" />
            </StackPanel>
          </Button>
          <Button Style="{StaticResource OpsNav}" Tag="Dispatch" Click="Module_Click">
            <StackPanel Orientation="Horizontal">
              <TextBlock Text="" FontFamily="Segoe MDL2 Assets" FontSize="21" Width="43" />
              <TextBlock Text="DISPATCH" VerticalAlignment="Center" />
            </StackPanel>
          </Button>
          <Button Style="{StaticResource OpsNav}" Tag="OCC" Click="Module_Click">
            <StackPanel Orientation="Horizontal">
              <TextBlock Text="" FontFamily="Segoe MDL2 Assets" FontSize="21" Width="43" />
              <TextBlock Text="OCC" VerticalAlignment="Center" />
            </StackPanel>
          </Button>
          <Button Style="{StaticResource OpsNav}" Tag="Reports" Click="Module_Click">
            <StackPanel Orientation="Horizontal">
              <TextBlock Text="" FontFamily="Segoe MDL2 Assets" FontSize="21" Width="43" />
              <TextBlock Text="REPORTS" VerticalAlignment="Center" />
            </StackPanel>
          </Button>
          <Button Style="{StaticResource OpsNav}" Tag="Settings" Click="Module_Click">
            <StackPanel Orientation="Horizontal">
              <TextBlock Text="" FontFamily="Segoe MDL2 Assets" FontSize="21" Width="43" />
              <TextBlock Text="SETTINGS" VerticalAlignment="Center" />
            </StackPanel>
          </Button>
        </StackPanel>
      </ScrollViewer>
    </DockPanel>
    <ScrollViewer x:Name="DashboardScroll" Grid.Row="1" Grid.Column="2" VerticalScrollBarVisibility="Disabled" HorizontalScrollBarVisibility="Disabled" PanningMode="None" Padding="0,0,1,8">
      <local:DashboardBodyPanel x:Name="DashboardBody" ViewportHeight="{Binding ViewportHeight, ElementName=DashboardScroll}">
        <Grid x:Name="HeroAlertsLayout">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2.7*" />
            <ColumnDefinition Width="12" />
            <ColumnDefinition Width="1*" />
          </Grid.ColumnDefinitions>
          <Border x:Name="HeroPanel" MinHeight="353" Style="{StaticResource OpsPanel}" ClipToBounds="True">
            <Grid>
              <Grid.Background><ImageBrush ImageSource="Assets/Dashboard/hero-unbranded.png" Stretch="UniformToFill" /></Grid.Background>
              <Rectangle>
                <Rectangle.Fill>
                  <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Color="#D8050D15" />
                    <GradientStop Color="#95040A10" Offset=".43" />
                    <GradientStop Color="#08000000" Offset="1" />
                  </LinearGradientBrush>
                </Rectangle.Fill>
              </Rectangle>
              <Rectangle>
                <Rectangle.Fill>
                  <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="#25000000" />
                    <GradientStop Color="#00000000" Offset=".45" />
                    <GradientStop Color="#F0040A0E" Offset="1" />
                  </LinearGradientBrush>
                </Rectangle.Fill>
              </Rectangle>
              <StackPanel x:Name="HeroQuote" HorizontalAlignment="Right" Margin="0,45,24,0">
                <TextBlock Text="“Same skies.&#xA; Higher standards.”" FontStyle="Italic" FontSize="17" />
                <Rectangle Fill="{StaticResource OpsYellow}" Width="31" Height="2" HorizontalAlignment="Left" Margin="12,15,0,0" />
              </StackPanel>
              <Grid Margin="20,15,20,12">
                <Grid.RowDefinitions>
                  <RowDefinition Height="28" />
                  <RowDefinition Height="51" />
                  <RowDefinition Height="80" />
                  <RowDefinition Height="36" />
                  <RowDefinition Height="69" />
                  <RowDefinition Height="42" />
                  <RowDefinition />
                </Grid.RowDefinitions>
                <Grid>
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="Auto" />
                  </Grid.ColumnDefinitions>
                  <TextBlock x:Name="HeroHeadingText" Text="YOUR NEXT FLIGHT" Style="{StaticResource OpsTitle}" FontSize="19" />
                  <TextBlock x:Name="TrackerModeText" Grid.Column="1" Text="REPLAY • SAMPLE DATA" Style="{StaticResource OpsCaption}" FontSize="9" VerticalAlignment="Top" />
                </Grid>
                <TextBlock x:Name="HeroFlightText" Grid.Row="1" Text="A601" FontFamily="Bahnschrift SemiCondensed" FontSize="48" FontWeight="Bold" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />
                <StackPanel Grid.Row="2" Orientation="Horizontal" VerticalAlignment="Center">
                  <StackPanel>
                    <TextBlock x:Name="OriginCodeText" Text="ORD" FontFamily="Bahnschrift SemiCondensed" FontSize="51" FontWeight="Bold" />
                    <TextBlock x:Name="OriginCityText" Text="CHICAGO" FontFamily="Bahnschrift SemiCondensed" FontSize="12" />
                  </StackPanel>
                  <StackPanel Orientation="Horizontal" Margin="23,0,23,12" VerticalAlignment="Center">
                    <Rectangle Height="2" Width="23" Fill="{StaticResource OpsYellow}" />
                    <Path Data="M 0,18 L 12,16 7,1 13,1 24,15 37,15 Q 48,17 37,20 L 24,20 13,34 7,34 12,21 0,20 Z" Fill="{StaticResource OpsYellow}" Width="44" Height="35" Stretch="Uniform" Margin="2,-4,2,0" />
                    <Rectangle Height="2" Width="42" Fill="#7F919E" />
                  </StackPanel>
                  <StackPanel>
                    <TextBlock x:Name="DestinationCodeText" Text="DTW" FontFamily="Bahnschrift SemiCondensed" FontSize="51" FontWeight="Bold" />
                    <TextBlock x:Name="DestinationCityText" Text="DETROIT" FontFamily="Bahnschrift SemiCondensed" FontSize="12" />
                  </StackPanel>
                </StackPanel>
                <StackPanel Grid.Row="3" Orientation="Horizontal" VerticalAlignment="Center">
                  <Border x:Name="HeroStatusBadge" Background="#163F24" BorderBrush="#2C7137" BorderThickness="1" CornerRadius="3" Padding="10,5">
                    <TextBlock x:Name="HeroStatusText" Text="●  ON TIME" Foreground="#7DEA57" FontFamily="Bahnschrift SemiCondensed" FontSize="13" />
                  </Border>
                  <TextBlock x:Name="HeroTimingText" Text="Scheduled 10:00Z" FontSize="13" Margin="13,0,0,0" VerticalAlignment="Center" MaxWidth="400" TextTrimming="CharacterEllipsis" />
                </StackPanel>
                <Grid Grid.Row="4" VerticalAlignment="Center">
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="92" />
                    <ColumnDefinition Width="60" />
                    <ColumnDefinition Width="92" />
                    <ColumnDefinition Width="60" />
                    <ColumnDefinition Width="*" />
                  </Grid.ColumnDefinitions>
                  <StackPanel>
                    <TextBlock Text="DEPARTURE" Style="{StaticResource OpsCaption}" />
                    <TextBlock x:Name="HeroDepartureText" FontFamily="Bahnschrift SemiCondensed" FontSize="19" Margin="0,3,0,0" />
                    <TextBlock Text="UTC" Style="{StaticResource OpsCaption}" FontSize="10" />
                  </StackPanel>
                  <StackPanel Grid.Column="1">
                    <TextBlock Text="GATE" Style="{StaticResource OpsCaption}" />
                    <TextBlock x:Name="HeroDepartureGateText" Text="—" FontSize="19" Margin="0,3,0,0" ToolTip="No gate assignment has been supplied." />
                  </StackPanel>
                  <StackPanel Grid.Column="2">
                    <TextBlock Text="ARRIVAL" Style="{StaticResource OpsCaption}" />
                    <TextBlock x:Name="HeroArrivalText" FontFamily="Bahnschrift SemiCondensed" FontSize="19" Margin="0,3,0,0" />
                    <TextBlock Text="UTC" Style="{StaticResource OpsCaption}" FontSize="10" />
                  </StackPanel>
                  <StackPanel Grid.Column="3">
                    <TextBlock Text="GATE" Style="{StaticResource OpsCaption}" />
                    <TextBlock x:Name="HeroArrivalGateText" Text="—" FontSize="19" Margin="0,3,0,0" ToolTip="No gate assignment has been supplied." />
                  </StackPanel>
                  <StackPanel Grid.Column="4">
                    <TextBlock Text="AIRCRAFT" Style="{StaticResource OpsCaption}" />
                    <TextBlock x:Name="AircraftText" FontSize="14" Margin="0,4,0,0" TextTrimming="CharacterEllipsis" />
                    <TextBlock x:Name="HeroAircraftTypeText" Text="SAMPLE ASSIGNMENT" Style="{StaticResource OpsCaption}" FontSize="10" Margin="0,3,0,0" />
                  </StackPanel>
                </Grid>
                <StackPanel Grid.Row="5" Orientation="Horizontal" VerticalAlignment="Center">
                  <Button x:Name="HeroFlightDetailsButton" Content="VIEW FLIGHT DETAILS    →" Style="{StaticResource OpsPrimary}" Click="FlightDetails_Click" Padding="17,9" />
                  <Button x:Name="HeroFlightDeckButton" Content="VIEW FLIGHT DECK" Style="{StaticResource OpsExit}" Click="FlightDeck_Click" Margin="12,0,0,0" Padding="22,9" />
                </StackPanel>
                <Grid x:Name="HeroProgressTrack" Grid.Row="6" Height="24" VerticalAlignment="Bottom">
                  <ProgressBar x:Name="ReplayProgress" Maximum="100" Height="3" VerticalAlignment="Center" Foreground="{StaticResource OpsYellow}" Background="#35414A" BorderThickness="0" />
                </Grid>
              </Grid>
            </Grid>
          </Border>
          <Border x:Name="AlertsPanel" Grid.Column="2" Style="{StaticResource OpsPanel}">
            <DockPanel>
              <Grid DockPanel.Dock="Top" Margin="13,8,10,7">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition />
                  <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="ALERTS" Style="{StaticResource OpsTitle}" />
                <Button x:Name="ViewAlertsButton" Grid.Column="1" Content="VIEW ALL (7)" Style="{StaticResource OpsLink}" Click="AllAlerts_Click" />
              </Grid>
              <TextBlock DockPanel.Dock="Bottom" Text="DEMO SCENARIO • NOT LIVE ADVISORIES" FontSize="8" Foreground="#8198AA" Margin="14,3,8,8" />
              <ItemsControl x:Name="AlertsItems" ItemTemplate="{StaticResource AlertTemplate}" />
            </DockPanel>
          </Border>
        </Grid>
        <Grid x:Name="ModulesMapLayout" Margin="0,12,0,12">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.85*" />
            <ColumnDefinition Width="1*" />
          </Grid.ColumnDefinitions>
          <ItemsControl x:Name="ModuleTiles">
            <ItemsControl.ItemsPanel>
              <ItemsPanelTemplate>
                <local:DashboardTilePanel />
              </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
              <DataTemplate>
                <Button Style="{StaticResource OpsTile}" Margin="0" VerticalContentAlignment="Stretch" Tag="{Binding Name}" Click="Module_Click" ToolTip="{Binding Description}">
                  <Grid MinHeight="113" ClipToBounds="True">
                    <Grid.Background><ImageBrush ImageSource="{Binding Image}" Stretch="UniformToFill" /></Grid.Background>
                    <Rectangle>
                      <Rectangle.Fill>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                          <GradientStop Color="#00000000" />
                          <GradientStop Color="#5A03080C" Offset=".4" />
                          <GradientStop Color="#FA03080C" Offset=".88" />
                        </LinearGradientBrush>
                      </Rectangle.Fill>
                    </Rectangle>
                    <Grid VerticalAlignment="Bottom" Margin="12,0,10,10">
                      <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition Width="Auto" />
                      </Grid.ColumnDefinitions>
                      <StackPanel>
                        <TextBlock Text="{Binding Label}" Style="{StaticResource OpsTitle}" FontSize="15" TextWrapping="Wrap" />
                        <TextBlock Text="{Binding Description}" Foreground="#B6C5D1" FontSize="11" Margin="0,4,0,0" TextWrapping="Wrap" />
                      </StackPanel>
                      <TextBlock Grid.Column="1" Text="→" Foreground="{StaticResource OpsYellow}" FontSize="22" VerticalAlignment="Bottom" />
                    </Grid>
                  </Grid>
                </Button>
              </DataTemplate>
            </ItemsControl.ItemTemplate>
          </ItemsControl>
          <Border x:Name="NetworkPanel" Grid.Column="1" Style="{StaticResource OpsPanel}">
            <DockPanel>
              <Grid DockPanel.Dock="Top" Margin="13,8">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition />
                  <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="NETWORK MAP" Style="{StaticResource OpsTitle}" />
                <Button x:Name="OpenTrackerButton" Grid.Column="1" Content="OPEN TRACKER  →" Style="{StaticResource OpsLink}" Foreground="#FFDA00" FontSize="10" Click="OpenTracker_Click" ToolTip="Open the full Flight Tracking workspace" />
              </Grid>
              <local:NetworkMap x:Name="RouteMap" />
            </DockPanel>
          </Border>
        </Grid>
        <Grid x:Name="SummaryLayout">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.65*" />
            <ColumnDefinition Width="12" />
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="12" />
            <ColumnDefinition Width="1*" />
          </Grid.ColumnDefinitions>
          <Border x:Name="OperationsPanel" MinHeight="225" Style="{StaticResource OpsPanel}">
            <DockPanel>
              <Grid DockPanel.Dock="Top" Margin="13,8,9,4">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition />
                  <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="OperationsHeading" Text="TODAY'S OPERATIONS" Style="{StaticResource OpsTitle}" />
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                  <Button x:Name="MyFlightsTab" Content="MY FLIGHTS" Tag="all" Style="{StaticResource OpsLink}" Foreground="{StaticResource OpsYellow}" Click="FlightsTab_Click" />
                  <Button x:Name="AssignedFlightsTab" Content="ASSIGNED" Tag="assigned" Style="{StaticResource OpsLink}" Foreground="#A7B7C5" Click="FlightsTab_Click" />
                  <Button x:Name="WatchlistTab" Content="WATCHLIST (0)" Tag="watchlist" Style="{StaticResource OpsLink}" Foreground="#A7B7C5" Click="FlightsTab_Click" />
                </StackPanel>
              </Grid>
              <Grid DockPanel.Dock="Bottom" Margin="12,4,9,5">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition />
                  <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="OperationsFootnote" Text="02 SEP 2026  •  ALL TIMES UTC" Style="{StaticResource OpsCaption}" FontSize="9" />
                <Button Grid.Column="1" x:Name="WatchFlightButton" Content="☆  WATCH SELECTED" Style="{StaticResource OpsLink}" Foreground="{StaticResource OpsYellow}" Click="WatchFlight_Click" />
              </Grid>
              <Grid>
                <DataGrid x:Name="DashboardFlightsGrid" Style="{StaticResource OpsGrid}" Margin="7,0" MouseDoubleClick="FlightRow_DoubleClick" SelectionChanged="FlightSelection_Changed">
                  <DataGrid.Columns>
                    <DataGridTextColumn Header="FLIGHT" Binding="{Binding Id}" Width="62" />
                    <DataGridTextColumn Header="ROUTE" Binding="{Binding Route}" Width="*" />
                    <DataGridTextColumn Header="OUT" Binding="{Binding Out}" Width="48" />
                    <DataGridTextColumn Header="IN" Binding="{Binding In}" Width="48" />
                    <DataGridTemplateColumn Header="STATUS" Width="80">
                      <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                          <Border Background="{Binding StatusBackground}" Padding="7,4" CornerRadius="3" HorizontalAlignment="Left">
                            <TextBlock Text="{Binding Status}" Foreground="{Binding StatusColor}" FontSize="10" />
                          </Border>
                        </DataTemplate>
                      </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                  </DataGrid.Columns>
                </DataGrid>
                <TextBlock x:Name="EmptyFlightsText" Visibility="Collapsed" Text="Select a flight in My flights, then add it to your watchlist." TextWrapping="Wrap" Foreground="#A3B5C5" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="22" />
              </Grid>
            </DockPanel>
          </Border>
          <Border x:Name="FleetPanel" MinHeight="225" Grid.Column="2" Style="{StaticResource OpsPanel}">
            <DockPanel>
              <Grid DockPanel.Dock="Top" Margin="13,8,10,0">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition />
                  <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="FLEET REFERENCE" Style="{StaticResource OpsTitle}" />
                <Button Grid.Column="1" Content="VIEW FLEET" Click="Fleet_Click" Style="{StaticResource OpsLink}" />
              </Grid>
              <TextBlock DockPanel.Dock="Bottom" Text="AIRFLEETS SNAPSHOT  ·  02 SEP 2026" FontSize="8" Foreground="#7F96A8" Margin="13,0,5,11" />
              <Grid Margin="9,7,9,5">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition />
                  <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <Viewbox Stretch="Uniform" MaxWidth="147">
                  <Grid Width="150" Height="150">
                    <Canvas x:Name="FleetRing" Width="150" Height="150" />
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                      <TextBlock x:Name="FleetCountText" Text="1,006" FontFamily="Bahnschrift SemiCondensed" FontSize="29" HorizontalAlignment="Center" />
                      <TextBlock Text="AIRCRAFT" Style="{StaticResource OpsCaption}" HorizontalAlignment="Center" />
                    </StackPanel>
                  </Grid>
                </Viewbox>
                <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="8,0,0,0">
                  <TextBlock x:Name="FleetActiveText" Text="■  ACTIVE        999" Foreground="#71D255" FontFamily="Bahnschrift SemiCondensed" FontSize="12" />
                  <TextBlock x:Name="FleetParkedText" Text="■  PARKED           7" Foreground="#FFD600" FontFamily="Bahnschrift SemiCondensed" FontSize="12" Margin="0,15,0,0" />
                  <TextBlock x:Name="FleetFamiliesText" Text="10 aircraft families" Style="{StaticResource OpsCaption}" FontSize="10" Margin="0,20,0,0" />
                  <TextBlock Text="Reference status only" Style="{StaticResource OpsCaption}" FontSize="9" Margin="0,5,0,0" />
                </StackPanel>
              </Grid>
            </DockPanel>
          </Border>
          <Border x:Name="MessagesPanel" MinHeight="225" Grid.Column="4" Style="{StaticResource OpsPanel}">
            <DockPanel>
              <Grid DockPanel.Dock="Top" Margin="13,8,10,5">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition />
                  <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="COMPANY MESSAGES" Style="{StaticResource OpsTitle}" />
                <Button Grid.Column="1" Content="VIEW ALL" Style="{StaticResource OpsLink}" Tag="Messages" Click="Module_Click" />
              </Grid>
              <ItemsControl x:Name="MessagesItems" ItemTemplate="{StaticResource MessageTemplate}" />
            </DockPanel>
          </Border>
        </Grid>
        <Border x:Name="AdvancedPanel" Style="{StaticResource OpsPanel}" Visibility="Collapsed" Margin="0,12,0,0" Padding="17">
          <StackPanel>
            <Grid>
              <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="Auto" />
              </Grid.ColumnDefinitions>
              <TextBlock Text="ROTATION DETAIL &amp; FLIGHT MILESTONES" Style="{StaticResource OpsTitle}" />
              <TextBlock x:Name="PhaseText" Grid.Column="1" Text="AT GATE" Foreground="{StaticResource OpsYellow}" FontFamily="Bahnschrift SemiCondensed" />
            </Grid>
            <DataGrid x:Name="RotationGrid" Style="{StaticResource OpsGrid}" Margin="0,13,0,8">
              <DataGrid.Columns>
                <DataGridTextColumn Header="FLIGHT" Binding="{Binding Id}" Width="80" />
                <DataGridTextColumn Header="ROUTE" Binding="{Binding Route}" Width="*" />
                <DataGridTextColumn Header="OUT UTC" Binding="{Binding Out}" Width="100" />
                <DataGridTextColumn Header="IN UTC" Binding="{Binding In}" Width="100" />
                <DataGridTextColumn Header="DEP / ARR DELAY" Binding="{Binding Delay}" Width="140" />
                <DataGridTextColumn Header="STATUS" Binding="{Binding Status}" Width="100" />
              </DataGrid.Columns>
            </DataGrid>
            <ItemsControl x:Name="EventsList">
              <ItemsControl.ItemTemplate>
                <DataTemplate>
                  <TextBlock Text="{Binding}" Foreground="#B9CBD8" FontSize="12" Margin="0,4" />
                </DataTemplate>
              </ItemsControl.ItemTemplate>
            </ItemsControl>
          </StackPanel>
        </Border>
      </local:DashboardBodyPanel>
    </ScrollViewer>
    <local:FlightTrackingWorkspace x:Name="FlightTrackingView" Grid.Row="1" Grid.Column="2" Visibility="Collapsed" Margin="0,0,1,8" />
    <local:PilotLogbookWorkspace x:Name="PilotLogbookView" Grid.Row="1" Grid.Column="2" Visibility="Collapsed" Margin="0,0,1,8" />
    <Grid x:Name="DashboardFooter" Grid.Row="2" Grid.ColumnSpan="3" Margin="220,0,0,0">
      <Grid.ColumnDefinitions>
        <ColumnDefinition />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>
      <TextBlock x:Name="FooterBrand" FontFamily="Bahnschrift SemiCondensed" FontSize="11" FontWeight="SemiBold" VerticalAlignment="Center">
        <Run Text="ALPHA 6" Foreground="{StaticResource OpsYellow}" /><Run Text=" OPS  •  OPERATIONS CONTROL CENTER" Foreground="#9DB0BE" />
      </TextBlock>
      <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
        <TextBlock x:Name="VersionText" FontFamily="Bahnschrift SemiCondensed" FontSize="11" FontWeight="SemiBold" Foreground="#9DB0BE" Margin="0,0,16,0" VerticalAlignment="Center" />
        <Button x:Name="ExitOpsButton" Content="EXIT OPS" Style="{StaticResource OpsExit}" Padding="16,8" MinWidth="88" MinHeight="34" Click="Exit_Click" AutomationProperties.Name="Exit Alpha 6 OPS" ToolTip="Close Alpha 6 OPS" />
      </StackPanel>
    </Grid>
    <Grid x:Name="ToolsOverlay" Grid.RowSpan="3" Grid.ColumnSpan="3" Visibility="Collapsed" Background="#AC000000" Panel.ZIndex="10">
      <Border Width="568" HorizontalAlignment="Right" Background="#0A131B" BorderBrush="#3E5262" BorderThickness="1,0,0,0">
        <DockPanel Margin="24">
          <Grid DockPanel.Dock="Top" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
              <ColumnDefinition />
              <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel>
              <TextBlock Text="YOUR FLIGHT DECK" Style="{StaticResource OpsTitle}" FontSize="25" />
              <TextBlock Text="Assignment, simulator connection &amp; live flight tracking" Style="{StaticResource OpsCaption}" Margin="0,7,0,0" />
            </StackPanel>
            <Button Grid.Column="1" Content="✕" Click="CloseTools_Click" Style="{StaticResource OpsButton}" AutomationProperties.Name="Close flight tools" />
          </Grid>
          <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel>
              <TextBlock Text="PILOT NAME" Style="{StaticResource OpsCaption}" />
              <TextBox x:Name="PilotNameBox" Style="{StaticResource OpsInput}" Margin="0,7,0,17" TextChanged="PilotName_Changed" AutomationProperties.Name="Pilot name" />
              <TextBlock Text="FLIGHT ASSIGNMENT" Style="{StaticResource OpsTitle}" />
              <TextBlock x:Name="RouteText" FontSize="23" Margin="0,8,0,4" />
              <TextBlock x:Name="DepartureText" Foreground="#A9BDCC" TextWrapping="Wrap" />
              <WrapPanel Margin="0,12,0,20">
                <Button Content="SET ACTIVE FLIGHT / IMPORT SIMBRIEF" Style="{StaticResource OpsButton}" Click="SetFlight_Click" Margin="0,0,10,0" />
                <Button x:Name="ClearFlightButton" Content="CLEAR ACTIVE FLIGHT" Style="{StaticResource OpsButton}" Click="ClearFlight_Click" IsEnabled="False" />
              </WrapPanel>
              <UniformGrid Columns="3" Margin="0,0,0,18">
                <StackPanel>
                  <TextBlock Text="DEPARTED" Style="{StaticResource OpsCaption}" />
                  <TextBlock x:Name="DepartedValueText" Text="—" FontSize="19" />
                </StackPanel>
                <StackPanel>
                  <TextBlock Text="EST. ARRIVAL" Style="{StaticResource OpsCaption}" />
                  <TextBlock x:Name="ArrivalValueText" Text="—" FontSize="19" />
                </StackPanel>
                <StackPanel>
                  <TextBlock Text="ELAPSED" Style="{StaticResource OpsCaption}" />
                  <TextBlock x:Name="ElapsedValueText" Text="—" FontSize="19" />
                </StackPanel>
              </UniformGrid>
              <TextBlock Text="SIMULATOR" Style="{StaticResource OpsTitle}" />
              <WrapPanel Margin="0,10,0,8">
                <Button x:Name="ConnectButton" Content="LAUNCH / CONNECT TO MSFS 2024" Style="{StaticResource OpsPrimary}" Click="Connect_Click" Margin="0,0,10,8" />
                <Button x:Name="ConnectFlightLabButton" Content="CONNECT TO FLIGHT LAB" Style="{StaticResource OpsButton}" Click="ConnectFlightLab_Click" Margin="0,0,10,8" ToolTip="Connect to the local Alpha 6 Flight Lab test program" />
                <Button x:Name="DisconnectButton" Content="DISCONNECT" Style="{StaticResource OpsButton}" IsEnabled="False" Click="Disconnect_Click" Margin="0,0,0,8" />
              </WrapPanel>
              <TextBlock x:Name="ConnectionText" Text="Launch or connect to MSFS 2024 here, then load a flight. For a virtual test, open Flight Lab and connect to it separately." TextWrapping="Wrap" Foreground="#B6C8D6" FontSize="12" />
              <TextBlock x:Name="LiveText" Text="Live telemetry appears after connecting." Foreground="#D9C956" TextWrapping="Wrap" FontSize="11" Margin="0,7,0,10" />
              <WrapPanel>
                <Button x:Name="LiveTimelineButton" Content="LIVE TIMELINE" Style="{StaticResource OpsButton}" IsEnabled="False" Click="LiveTimeline_Click" Margin="0,0,8,8" />
                <Button x:Name="LiveDebriefButton" Content="LIVE DEBRIEF" Style="{StaticResource OpsButton}" IsEnabled="False" Click="LiveDebrief_Click" Margin="0,0,0,8" />
              </WrapPanel>
              <Rectangle Height="1" Fill="#2B3E4B" Margin="0,13,0,20" />
              <TextBlock Text="FLIGHT DISPLAY" Style="{StaticResource OpsTitle}" />
              <TextBlock x:Name="StatusText" TextWrapping="Wrap" Foreground="#ADC0CE" FontSize="12" Margin="0,4,0,16" />
              <WrapPanel>
                <Button x:Name="SimpleButton" Content="DASHBOARD" Style="{StaticResource OpsButton}" Click="Simple_Click" Margin="0,0,8,8" />
                <Button x:Name="AdvancedButton" Content="SHOW ROTATION DETAIL" Style="{StaticResource OpsButton}" Click="Advanced_Click" Margin="0,0,0,8" />
              </WrapPanel>
              <Rectangle Height="55" HorizontalAlignment="Left" Width="190" Margin="0,20,0,5">
                <Rectangle.Fill>
                  <ImageBrush ImageSource="Assets/Dashboard/alpha6-designs-logo.png" Viewbox="0.01,0.12,0.98,0.77" Stretch="Uniform" />
                </Rectangle.Fill>
              </Rectangle>
              <!-- Replay remains an internal deterministic test fixture, not a pilot-facing tool. -->
              <Grid Visibility="Collapsed">
                <ComboBox x:Name="FixtureCombo" />
                <Button x:Name="ReplayButton" />
                <Button x:Name="ResetButton" />
                <TextBlock x:Name="LogStatusText" Text="Test logs save automatically when you connect." />
                <TextBlock x:Name="ProgramHealthText" Text="PROGRAM MONITOR STARTING" />
              </Grid>
            </StackPanel>
          </ScrollViewer>
        </DockPanel>
      </Border>
    </Grid>
    </Grid>
    </Viewbox>
  </Grid>
</Window>
